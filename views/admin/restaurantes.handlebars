<h2>Lista de Restaurantes</h2>
<hr>
<a href="/admin/restaurantes/add"><button class = "btn btn-success">Novo Restaurante</button></a>

{{#each restaurantes}}
    <div class="card mt-4">
        <div class="card-body ">
            <h4>{{nome}}</h4>
            <p>la description: {{descricao}}</p>
            <small>Data: {{data}}</small><br>
            <small>Catégorie: {{categoria.nome}}</small>
            <br>
            <a href="/admin/restaurantes/edit/{{_id}}"><button class="btn btn-success mt-4">Editar restaurante</button></a>
            <form action="/admin/restaurantes/deletar" method = "POST">
                <input type= "hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger mt-2">Deletar restaurante</button>
            </form>
        </div>
    </div>
{{else}}
    <p>Nenhuma restaurante registrado!</p>
{{/each}}
